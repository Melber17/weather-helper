import React from "react";
import styled from "styled-components/native";

export const TodaysForecastCard: React.FC = () => {
	return (
		<Wrapper>

		</Wrapper>
	);
};

const Wrapper = styled.View``;
